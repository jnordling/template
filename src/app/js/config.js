export const config = {
  app: {
    rootClassName: 'root',
    dojoTheme: 'claro'
  },
  map: {
    id: 'map',
    dojoTheme: 'claro'
  }
}
export const app = config.app
export const map = config.map
